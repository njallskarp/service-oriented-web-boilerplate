import { expect } from 'chai';

/**
 * For `npm test` to work there needs to be at least one unit test file.
 *
 * Remove this file once an actual unit test file is added.
 *
 */

describe('#isProductionEnvironment', function () {
	it('should return true if NODE_ENV is set to "production"', function () {
		expect(4).to.equal(4);
	});
});
